<resources>

    <!-- Base application theme. -->
    <style name="MusicaleTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Main theme colors -->
        <item name="android:colorActivatedHighlight">@color/accent</item>

        <item name="android:color">@color/primary</item>

        <item name="android:dropDownListViewStyle">@style/actionBarSpinnerStyle</item>

        <item name="vpiTabPageIndicatorStyle">@style/Widget.TabPageIndicator</item>

        <item name="android:actionBarStyle">@style/MusicaleActionBar</item>

    </style>

    <!-- Base application theme for when no actionBar is wanted. -->
    <style name="MusicaleThemeNoActionBar" parent="android:Theme.Holo.Light.NoActionBar">
        <!-- Main theme colors -->
        <item name="android:color">@color/primary</item>

        <item name="android:colorActivatedHighlight">@color/accent</item>

        <item name="vpiTabPageIndicatorStyle">@style/Widget.TabPageIndicator</item>

    </style>

    <style name="actionBarSpinnerStyle" parent="android:style/Widget.ListView.DropDown">
        <item name="android:divider">#ffe4e4e4</item>
        <item name="android:dividerHeight">.25dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:width">168dp</item>
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">@drawable/events_view_selector</item>
    </style>

    <style name="Widget.TabPageIndicator" parent="Widget">
        <item name="android:gravity">center</item>
        <item name="android:color">#ffb9b9b9</item>
        <item name="android:background">@drawable/tab_page_indicator</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:tabStripEnabled">true</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAppearance">@style/TextAppearance.TabPageIndicator</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="TextAppearance.TabPageIndicator" parent="Widget">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@drawable/tab_page_indicator_title_colors</item>
    </style>

    <style name="MusicaleActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/primary</item>
    </style>

</resources>